// Single source of truth for the legal pages (Terms/Privacy/Refund), so
// the contact address and "last updated" date only need to change in one
// place. No official business address, entity name, or registration
// number exists anywhere else in this project, so the legal pages don't
// reference one — see the summary this task returns for what still needs
// to be filled in before submitting to Paddle.
export const SUPPORT_EMAIL = "support@fidmap.co";

// Bump this whenever the legal page content actually changes.
export const LEGAL_LAST_UPDATED = "September 10, 2026";
